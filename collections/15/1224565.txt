where L(0) is the likelihood of the model with only the intercept,      L (    θ ^    )    {\displaystyle {L({\hat {\theta }})}}   is the likelihood of the estimated model (i.e., the model with a given set of parameter estimates) and n is the sample size.